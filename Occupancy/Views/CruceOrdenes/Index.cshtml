@model IEnumerable<Occupancy.Models.CruceOrden>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IDEspacio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IDTipoMov)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IDArbitrioMov)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IDEspacio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IDTipoMov)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IDArbitrioMov)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
