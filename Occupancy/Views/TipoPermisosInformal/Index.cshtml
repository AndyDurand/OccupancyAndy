@model IEnumerable<Occupancy.Models.TipoPermisoInformal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PermisoInformal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PermisoInformal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDTipoPermisoInformal }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDTipoPermisoInformal }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDTipoPermisoInformal })
        </td>
    </tr>
}

</table>
