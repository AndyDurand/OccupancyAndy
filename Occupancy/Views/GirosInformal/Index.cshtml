@model IEnumerable<Occupancy.Models.GiroInformal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GiroInformal1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GiroInformal1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDGiroInformal }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDGiroInformal }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDGiroInformal })
        </td>
    </tr>
}

</table>
